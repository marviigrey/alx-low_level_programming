#include <stdio.h>
/**
 * main- calls out function and prints stdout
 * return-returns 0 value (success)
 */
int main(void)	{
	printf("with proper grammar, but the outcome is a piece of art,
			");
		return(0);
}
